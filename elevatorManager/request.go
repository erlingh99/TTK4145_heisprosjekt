package elevatorManager

import (
	"../elevio"
 	"../config"
)


func request_above(Elevator e) {
	for (f := e.curFloor; f < N_FLOORS; f++) {
		for (btn := elevio.ButtonType(0); btn < N_BUTTONS; btn++) {
			 if ()
		}
	}
}

